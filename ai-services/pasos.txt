ejecutar pip install -r requirements.txt para instalar dependencias

esta configurado par usar llama2

para que corra el proyecto uvicorn main:app --reload --host 127.0.0.1 --port 8000



http://127.0.0.1:8000/docs  para acceder a swagger